import * as React from "react";
import { Routes, Route } from "react-router-dom";

interface Props {}

const AuthFeatures: React.FunctionComponent<Props> = (props) => {
  return <>aaa</>;
};

export default AuthFeatures;
